;;; `(upcase (file-name-nondirectory (file-name-sans-extension (buffer-file-name))))` --- $1 -*- lexical-binding: t -*-
;;
;; Author: `user-full-name` <`user-mail-address`>
;; Copyright © `(format-time-string "%Y")` `user-full-name`. all rights reserved.
;; URL: `user-website`
;; Created at: `(format-time-string "%e %B %Y")`
;;
;;; Commentary:
;;
;; $2
;;
;;; Code:

;;(setq debug-on-error t)

$0
(provide '`(file-name-nondirectory (file-name-sans-extension (buffer-file-name)))`)
;;; `(file-name-nondirectory (buffer-file-name))` ends here
